# 📚 Подробная инструкция по запуску и деплою бота

## 🎯 Что нужно для запуска

1. **Python 3.8+** установлен на вашем компьютере
2. **Telegram Bot Token** от @BotFather
3. **Кинопоиск API Key** от kinopoisk.dev
4. **Домен с HTTPS** (для мини-приложения) - можно использовать бесплатные сервисы
5. **GitHub аккаунт** (для деплоя на Render/Heroku)

## 🚀 Быстрый старт (Render.com) ⭐

**Самый простой способ деплоя!** Перейдите к разделу [Деплой на Render.com](#-деплой-на-rendercom-пошагово--рекомендуется) ниже.

---

## 📦 Вариант 1: Локальный запуск (для тестирования)

### Шаг 1: Установка зависимостей

```bash
# Установите все необходимые библиотеки
pip install -r requirements.txt
```

### Шаг 2: Настройка .env файла

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_бота_от_BotFather
KINOPOISK_API_KEY=ваш_api_ключ_от_kinopoisk.dev
```

### Шаг 3: Запуск бота

```bash
# Запустите бота
python bot.py
```

**Примечание:** Мини-приложение не будет работать локально, так как Telegram требует HTTPS. Используйте этот вариант только для тестирования основного функционала бота.

---

## 🌐 Вариант 2: Деплой на сервер (рекомендуется)

### Где можно разместить бота:

#### 1. **Heroku** (бесплатно, легко)
- Регистрация: https://www.heroku.com/
- Поддержка Python и веб-серверов
- Автоматический HTTPS
- Простой деплой через Git

#### 2. **Railway** (бесплатно) ⭐
- Регистрация: https://railway.app/
- Автоматический деплой
- HTTPS включен
- Подробная инструкция ниже

#### 3. **Render** (бесплатно) ⭐ РЕКОМЕНДУЕТСЯ
- Регистрация: https://render.com/
- Простой деплой через GitHub
- Автоматический HTTPS
- Бесплатный план доступен
- Подробная инструкция ниже

#### 4. **VPS сервер** (DigitalOcean, AWS, Vultr и др.)
- Полный контроль
- Требует настройки

---

## 🚀 Деплой на Heroku (пошагово)

### Шаг 1: Подготовка проекта

1. Создайте файл `Procfile` в корне проекта:

```
worker: python bot.py
web: python web_server.py
```

2. Создайте файл `runtime.txt`:

```
python-3.11.0
```

3. Обновите `bot.py` - замените URL в строке с `web_app_url`:

```python
# Найдите строку:
web_app_url = "https://your-domain.com/webapp/index.html"

# Замените на ваш Heroku URL (будет после деплоя):
web_app_url = "https://your-app-name.herokuapp.com/"
```

4. Обновите `webapp/app.js` - замените API_URL:

```javascript
// Найдите строку:
const API_URL = 'https://your-domain.com/api';

// Замените на:
const API_URL = 'https://your-app-name.herokuapp.com/api';
```

### Шаг 2: Установка Heroku CLI

1. Установите Heroku CLI: https://devcenter.heroku.com/articles/heroku-cli

2. Войдите в Heroku:
```bash
heroku login
```

### Шаг 3: Создание приложения

```bash
# Создайте новое приложение
heroku create your-app-name

# Добавьте переменные окружения
heroku config:set BOT_TOKEN=ваш_токен_бота
heroku config:set KINOPOISK_API_KEY=ваш_api_ключ
```

### Шаг 4: Деплой

```bash
# Инициализируйте Git (если еще не сделано)
git init
git add .
git commit -m "Initial commit"

# Деплой на Heroku
git push heroku main
```

### Шаг 5: Запуск воркеров

```bash
# Запустите бота
heroku ps:scale worker=1

# Запустите веб-сервер
heroku ps:scale web=1
```

### Шаг 6: Обновление URL в коде

После деплоя получите URL вашего приложения и обновите:
1. `bot.py` - строка с `web_app_url`
2. `webapp/app.js` - строка с `API_URL`

Затем сделайте еще один коммит и деплой.

---

## 🚀 Деплой на Railway (пошагово) ⭐

Railway - простой и быстрый способ деплоя с автоматическим HTTPS и бесплатным планом.

### Шаг 1: Подготовка проекта

1. **Убедитесь, что у вас есть GitHub репозиторий:**
   - Если нет, создайте репозиторий на GitHub
   - Загрузите код в репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
   git push -u origin main
   ```

2. **Обновите URL в коде (пока оставьте заглушки, обновим после деплоя):**
   - В `bot.py` найдите строку с `web_app_url` (около строки 60)
   - В `webapp/app.js` найдите строку с `API_URL` (около строки 7)
   - Пока оставьте как есть, обновим после получения URL от Railway

### Шаг 2: Регистрация на Railway

1. Перейдите на https://railway.app/
2. Нажмите **"Start a New Project"** или **"Login"**
3. Выберите способ регистрации:
   - **GitHub** (рекомендуется) - нажмите "Login with GitHub"
   - **Email** - введите email и пароль
4. Подтвердите email, если регистрировались через email

### Шаг 3: Создание проекта

1. В Dashboard Railway нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Если первый раз:
   - Нажмите **"Configure GitHub App"**
   - Выберите репозиторий с ботом
   - Нажмите **"Install"**
4. Выберите ваш репозиторий из списка
5. Railway автоматически определит Python проект

### Шаг 4: Создание Web Service (для веб-сервера)

1. В вашем проекте Railway нажмите **"+ New"** → **"Service"**
2. Выберите **"GitHub Repo"** и выберите ваш репозиторий
3. Railway автоматически начнет деплой

4. **Настройте Service:**
   - Railway автоматически определит Python
   - Если нет, в настройках Service:
     - **Build Command:** `pip install -r requirements.txt`
     - **Start Command:** `python web_server.py`

5. **Добавьте Environment Variables (переменные окружения):**
   - В настройках Service нажмите **"Variables"**
   - Добавьте переменные:
     - `BOT_TOKEN` = ваш токен от BotFather
     - `KINOPOISK_API_KEY` = ваш API ключ от kinopoisk.dev
     - `PORT` = Railway автоматически установит, но можно указать явно

6. **Настройте домен и получите URL:**
   - В настройках Service нажмите **"Settings"** → **"Generate Domain"**
   - Railway создаст домен вида: `your-service-name.up.railway.app`
   - **Где найти URL:**
     - В Dashboard Railway выберите ваш Web Service
     - В верхней части страницы Service вы увидите раздел **"Domains"**
     - Там будет указан URL вида: `https://your-service-name.up.railway.app`
     - Или в настройках Service → **"Settings"** → **"Networking"** → там будет ваш домен
   - Или используйте свой домен: **"Custom Domain"**

7. **Дождитесь деплоя:**
   - Railway начнет сборку и деплой
   - Это займет 2-5 минут
   - Вы увидите логи в реальном времени
   - После успешного деплоя статус изменится на "Active"

### Шаг 5: Создание Background Worker (для бота)

1. В вашем проекте Railway нажмите **"+ New"** → **"Service"**
2. Выберите **"GitHub Repo"** и выберите тот же репозиторий
3. **Настройте Service:**
   - В настройках Service:
     - **Build Command:** `pip install -r requirements.txt`
     - **Start Command:** `python bot.py`
   - **КРИТИЧЕСКИ ВАЖНО для Python версии:**
     - Railway может использовать Python 3.13, который несовместим с `python-telegram-bot==20.7`
     - **Убедитесь, что файлы `nixpacks.toml` и `.python-version` закоммичены и запушены в GitHub**
     - Файл `nixpacks.toml` уже создан в репозитории - он заставит Railway использовать Python 3.11
     - После `git push` Railway должен автоматически перезапустить деплой с Python 3.11
     - Если проблема сохраняется, см. раздел "Решение проблем" ниже

4. **Добавьте те же Environment Variables:**
   - В настройках Service нажмите **"Variables"**
   - Добавьте:
     - `BOT_TOKEN` = ваш токен от BotFather
     - `KINOPOISK_API_KEY` = ваш API ключ от kinopoisk.dev

5. **Дождитесь деплоя** (2-5 минут)

### Шаг 6: Обновление URL в коде

После успешного деплоя Web Service вам нужно найти и скопировать URL.

**Где найти Railway URL:**

1. **В Dashboard Railway:**
   - Откройте ваш проект в Dashboard
   - Выберите ваш **Web Service** (не Background Worker!)
   - В верхней части страницы Service найдите раздел **"Domains"** или **"Networking"**
   - Там будет указан URL вида: `https://your-service-name.up.railway.app`
   - **Скопируйте этот URL** (без слеша в конце)

2. **Альтернативный способ:**
   - В настройках Service → **"Settings"** → **"Networking"**
   - Там будет раздел **"Public Domain"** с вашим URL
   - Или нажмите **"Generate Domain"** если домен еще не создан

3. **Пример URL:**
   - `https://tgbot-web-production.up.railway.app`
   - `https://web-service-1234.up.railway.app`
   - Или ваш кастомный домен, если настроили

**Важно:** Используйте URL от **Web Service**, а не от Background Worker!

Теперь обновите код:

1. **Обновите `bot.py`:**
   - Найдите строку (около строки 60):
     ```python
     web_app_url = "https://your-domain.com/webapp/index.html"
     ```
   - Замените на ваш Railway URL (скопируйте из Dashboard):
     ```python
     web_app_url = "https://your-service-name.up.railway.app/"
     ```
   - **Пример:** Если ваш URL `https://tgbot-web-production.up.railway.app`, то:
     ```python
     web_app_url = "https://tgbot-web-production.up.railway.app/"
     ```
   - Сохраните файл

2. **Обновите `webapp/app.js`:**
   - Найдите строку (около строки 7):
     ```javascript
     const API_URL = 'https://your-domain.com/api';
     ```
   - Замените на ваш Railway URL (скопируйте из Dashboard):
     ```javascript
     const API_URL = 'https://your-service-name.up.railway.app/api';
     ```
   - **Пример:** Если ваш URL `https://tgbot-web-production.up.railway.app`, то:
     ```javascript
     const API_URL = 'https://tgbot-web-production.up.railway.app/api';
     ```
   - **Важно:** Используйте тот же URL, что и в `bot.py`, только добавьте `/api` в конце
   - Сохраните файл

3. **Закоммитьте и запушьте изменения:**
   ```bash
   git add bot.py webapp/app.js
   git commit -m "Update URLs for Railway deployment"
   git push
   ```

4. **Railway автоматически перезапустит сервисы** после push в GitHub

### Шаг 7: Настройка Web App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Выберите **"Configure Menu Button"**
6. Введите текст кнопки: `Открыть приложение` (или любой другой)
7. Введите URL: `https://your-service-name.up.railway.app/` (ваш Railway URL)
8. Нажмите **"Done"**

### Шаг 8: Проверка работы

1. **Проверьте Web Service:**
   - Откройте ваш URL в браузере (скопируйте из Dashboard Railway → ваш Web Service → Domains)
   - Должна открыться страница мини-приложения
   - Проверьте API: `https://ваш-url.up.railway.app/api/get_movie?user_id=123`
   - Замените `ваш-url` на ваш реальный URL из Dashboard

2. **Проверьте бота:**
   - Откройте бота в Telegram
   - Отправьте `/start`
   - Проверьте, что меню открывается
   - Нажмите кнопку "Открыть мини-приложение"
   - Проверьте, что приложение открывается

3. **Проверьте избранное:**
   - Добавьте фильм в избранное через бота
   - Откройте раздел "Избранное"
   - Проверьте, что фильм сохранился

### Шаг 9: Мониторинг и логи

1. **Просмотр логов:**
   - В Dashboard Railway выберите ваш Service
   - Перейдите на вкладку **"Deployments"** → выберите последний деплой → **"View Logs"**
   - Или в реальном времени: **"Logs"** в боковом меню Service

2. **Проверка статуса:**
   - В Dashboard все сервисы должны быть в статусе **"Active"** (зеленый индикатор)
   - Если сервис не запускается, проверьте логи на ошибки

### ⚠️ Важные замечания для Railway:

1. **Бесплатный план:**
   - $5 бесплатных кредитов в месяц
   - Автоматическое отключение после исчерпания кредитов
   - Для продакшена рассмотрите платный план ($5/месяц)

2. **Ограничения бесплатного плана:**
   - Ограниченное количество часов работы
   - Автоматическое отключение при неактивности (на некоторых планах)
   - Для постоянной работы нужен платный план

3. **База данных:**
   - Файл `favorites.db` будет создаваться автоматически
   - На бесплатном плане данные могут теряться при перезапуске
   - Для продакшена используйте PostgreSQL (Railway предоставляет плагин)

4. **Обновление кода:**
   - Просто делайте `git push` в GitHub
   - Railway автоматически обнаружит изменения и перезапустит сервисы
   - Это занимает 2-5 минут

5. **Переменная PORT:**
   - Railway автоматически устанавливает переменную `PORT`
   - Веб-сервер уже настроен для работы с этой переменной

### 🔧 Решение проблем на Railway:

**Проблема: Сервис не запускается**
- Проверьте логи в Dashboard → ваш Service → Logs
- Убедитесь, что все переменные окружения установлены
- Проверьте, что `requirements.txt` содержит все зависимости
- Убедитесь, что Start Command правильный

**Проблема: Ошибка "mise ERROR Failed to install core:python@3.11.0"**
- Railway пытается использовать `mise` для установки Python из `runtime.txt`
- **Решение 1:** Удалите файл `runtime.txt` (он для Heroku, Railway определит Python автоматически)
- **Решение 2:** Измените формат в `runtime.txt` с `python-3.11.0` на `3.11.0`
- **Решение 3:** Создайте файл `.python-version` с содержимым `3.11.0`
- Railway автоматически определит Python версию из `requirements.txt` или `.python-version`

**Проблема: 502 Bad Gateway**
- Сервис может быть в процессе деплоя
- Подождите 1-2 минуты и обновите страницу
- Проверьте логи на ошибки

**Проблема: Мини-приложение не открывается**
- Убедитесь, что URL правильный в BotFather
- Проверьте, что Web Service запущен (статус "Active")
- Проверьте консоль браузера (F12) на ошибки
- Убедитесь, что используется HTTPS (не HTTP)

**Проблема: Бот не отвечает**
- Проверьте логи Background Worker
- Убедитесь, что `BOT_TOKEN` правильный
- Проверьте, что Worker запущен (статус "Active")
- Убедитесь, что переменные окружения установлены для Worker

**Проблема: Недостаточно кредитов**
- Проверьте использование в Dashboard → Usage
- Добавьте способ оплаты для увеличения лимитов
- Или перейдите на платный план

### 📸 Скриншоты процесса (для справки):

1. **Dashboard Railway:** https://railway.app/dashboard
   - После входа вы увидите список ваших проектов
   - Зеленый индикатор = сервис работает
   - Желтый/красный = есть проблемы

2. **Создание проекта:**
   - New Project → Deploy from GitHub repo
   - Выберите репозиторий

3. **Создание Service:**
   - + New → Service
   - GitHub Repo → выберите репозиторий
   - Настройте Build Command и Start Command

4. **Environment Variables:**
   - Settings → Variables → Add Variable
   - Добавьте BOT_TOKEN и KINOPOISK_API_KEY

5. **Генерация домена:**
   - Settings → Generate Domain
   - Или используйте Custom Domain

### 🎯 Быстрая проверка после деплоя:

```bash
# 1. Проверьте Web Service (замените на ваш URL)
curl https://your-service-name.up.railway.app/

# 2. Проверьте API
curl https://your-service-name.up.railway.app/api/get_movie?user_id=123

# 3. Проверьте бота в Telegram
# Отправьте /start и проверьте ответ
```

### 💡 Полезные команды для Railway:

- **Автоматический деплой:** Просто делайте `git push` в GitHub
- **Ручной деплой:** Dashboard → ваш Service → Deployments → Redeploy
- **Просмотр логов:** Dashboard → ваш Service → Logs
- **Остановка сервиса:** Dashboard → ваш Service → Settings → Pause
- **Удаление сервиса:** Dashboard → ваш Service → Settings → Delete
- **Просмотр использования:** Dashboard → Usage

### 🔄 Миграция с локального на Railway:

Если вы уже запускали бота локально:

1. **Экспортируйте базу данных (опционально):**
   ```bash
   # Скопируйте favorites.db если нужно сохранить данные
   cp favorites.db backups/favorites.db
   ```

2. **Создайте .railwayignore (опционально):**
   ```
   __pycache__/
   *.pyc
   .env
   *.db
   ```

3. **Следуйте инструкции выше** для деплоя на Railway

---

## 🚀 Деплой на Render.com (пошагово) ⭐ РЕКОМЕНДУЕТСЯ

Render.com - один из самых простых способов деплоя с автоматическим HTTPS и бесплатным планом.

### Шаг 1: Подготовка проекта

1. **Убедитесь, что у вас есть GitHub репозиторий:**
   - Если нет, создайте репозиторий на GitHub
   - Загрузите код в репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
   git push -u origin main
   ```

2. **Обновите URL в коде (пока оставьте заглушки, обновим после деплоя):**
   - В `bot.py` найдите строку с `web_app_url` (около строки 60)
   - В `webapp/app.js` найдите строку с `API_URL` (около строки 7)
   - Пока оставьте как есть, обновим после получения URL от Render

### Шаг 2: Регистрация на Render

1. Перейдите на https://dashboard.render.com/
2. Нажмите **"Get Started for Free"** или **"Sign Up"**
3. Зарегистрируйтесь через GitHub (рекомендуется) или email
4. Подтвердите email, если регистрировались через email

### Шаг 3: Создание Web Service (для веб-сервера)

1. В Dashboard Render нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий:
   - Если первый раз: нажмите **"Connect account"** и авторизуйтесь
   - Выберите ваш репозиторий с ботом
   - Нажмите **"Connect"**

3. **Настройте Web Service:**
   - **Name:** `tgbot-web` (или любое другое имя)
   - **Region:** выберите ближайший (например, Frankfurt для Европы)
   - **Branch:** `main` (или ваша основная ветка)
   - **Root Directory:** оставьте пустым
   - **Runtime:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python web_server.py`
   - **Instance Type:** выберите **"Free"** (для начала)

4. **Добавьте Environment Variables (переменные окружения):**
   - Нажмите **"Advanced"** → **"Add Environment Variable"**
   - Добавьте:
     - `BOT_TOKEN` = ваш токен от BotFather
     - `KINOPOISK_API_KEY` = ваш API ключ от kinopoisk.dev
   - Нажмите **"Add Environment Variable"** для каждой переменной

5. Нажмите **"Create Web Service"**

6. **Дождитесь деплоя:**
   - Render начнет сборку и деплой
   - Это займет 2-5 минут
   - Вы увидите логи в реальном времени
   - После успешного деплоя вы получите URL вида: `https://tgbot-web.onrender.com`

### Шаг 4: Создание Background Worker (для бота)

1. В Dashboard Render нажмите **"New +"** → **"Background Worker"**

2. **Настройте Background Worker:**
   - **Name:** `tgbot-bot` (или любое другое имя)
   - **Region:** тот же, что и для Web Service
   - **Branch:** `main`
   - **Root Directory:** оставьте пустым
   - **Runtime:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python bot.py`
   - **Instance Type:** выберите **"Free"**

3. **Добавьте те же Environment Variables:**
   - `BOT_TOKEN` = ваш токен от BotFather
   - `KINOPOISK_API_KEY` = ваш API ключ от kinopoisk.dev

4. Нажмите **"Create Background Worker"**

5. **Дождитесь деплоя** (2-5 минут)

### Шаг 5: Обновление URL в коде

После успешного деплоя Web Service вы получите URL (например, `https://tgbot-web.onrender.com`).

1. **Обновите `bot.py`:**
   - Найдите строку (около строки 60):
     ```python
     web_app_url = "https://your-domain.com/webapp/index.html"
     ```
   - Замените на ваш Render URL:
     ```python
     web_app_url = "https://tgbot-web.onrender.com/"
     ```
   - Сохраните файл

2. **Обновите `webapp/app.js`:**
   - Найдите строку (около строки 7):
     ```javascript
     const API_URL = 'https://your-domain.com/api';
     ```
   - Замените на ваш Render URL:
     ```javascript
     const API_URL = 'https://tgbot-web.onrender.com/api';
     ```
   - Сохраните файл

3. **Закоммитьте и запушьте изменения:**
   ```bash
   git add bot.py webapp/app.js
   git commit -m "Update URLs for Render deployment"
   git push
   ```

4. **Render автоматически перезапустит сервисы** после push в GitHub

### Шаг 6: Настройка Web App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Выберите **"Configure Menu Button"**
6. Введите текст кнопки: `Открыть приложение` (или любой другой)
7. Введите URL: `https://tgbot-web.onrender.com/` (ваш Render URL)
8. Нажмите **"Done"**

### Шаг 7: Проверка работы

1. **Проверьте Web Service:**
   - Откройте ваш URL в браузере: `https://tgbot-web.onrender.com/`
   - Должна открыться страница мини-приложения
   - Проверьте API: `https://tgbot-web.onrender.com/api/get_movie?user_id=123`

2. **Проверьте бота:**
   - Откройте бота в Telegram
   - Отправьте `/start`
   - Проверьте, что меню открывается
   - Нажмите кнопку "Открыть мини-приложение"
   - Проверьте, что приложение открывается

3. **Проверьте избранное:**
   - Добавьте фильм в избранное через бота
   - Откройте раздел "Избранное"
   - Проверьте, что фильм сохранился

### Шаг 8: Мониторинг и логи

1. **Просмотр логов:**
   - В Dashboard Render выберите ваш сервис
   - Перейдите на вкладку **"Logs"**
   - Вы увидите логи в реальном времени

2. **Проверка статуса:**
   - В Dashboard все сервисы должны быть в статусе **"Live"** (зеленый индикатор)
   - Если сервис не запускается, проверьте логи на ошибки

### ⚠️ Важные замечания для Render:

1. **Бесплатный план:**
   - Сервисы "засыпают" после 15 минут неактивности
   - Первый запрос после "сна" может занять 30-60 секунд (холодный старт)
   - Это нормально для бесплатного плана

2. **Ограничения бесплатного плана:**
   - 750 часов работы в месяц (достаточно для тестирования)
   - Автоматическое "засыпание" после неактивности
   - Для продакшена рассмотрите платный план ($7/месяц)

3. **База данных:**
   - Файл `favorites.db` будет создаваться автоматически
   - На бесплатном плане данные могут теряться при перезапуске
   - Для продакшена используйте PostgreSQL (Render предоставляет бесплатный план)

4. **Обновление кода:**
   - Просто делайте `git push` в GitHub
   - Render автоматически обнаружит изменения и перезапустит сервисы
   - Это занимает 2-5 минут

### 🔧 Решение проблем на Render:

**Проблема: Сервис не запускается**
- Проверьте логи в Dashboard → Logs
- Убедитесь, что все переменные окружения установлены
- Проверьте, что `requirements.txt` содержит все зависимости

**Проблема: 502 Bad Gateway**
- Сервис может "спать" (бесплатный план)
- Подождите 30-60 секунд и обновите страницу
- Или отправьте запрос боту, чтобы "разбудить" сервис

**Проблема: Мини-приложение не открывается**
- Убедитесь, что URL правильный в BotFather
- Проверьте, что Web Service запущен (статус "Live")
- Проверьте консоль браузера (F12) на ошибки

**Проблема: Бот не отвечает**
- Проверьте логи Background Worker
- Убедитесь, что `BOT_TOKEN` правильный
- Проверьте, что Worker запущен (статус "Live")

### 📸 Скриншоты процесса (для справки):

1. **Dashboard Render:** https://dashboard.render.com/
   - После входа вы увидите список ваших сервисов
   - Зеленый индикатор = сервис работает
   - Желтый/красный = есть проблемы

2. **Создание Web Service:**
   - New + → Web Service
   - Подключите GitHub репозиторий
   - Заполните настройки (см. Шаг 3 выше)

3. **Создание Background Worker:**
   - New + → Background Worker
   - Те же настройки, но Start Command: `python bot.py`

4. **Environment Variables:**
   - Advanced → Add Environment Variable
   - Добавьте BOT_TOKEN и KINOPOISK_API_KEY

### 🎯 Быстрая проверка после деплоя:

```bash
# 1. Проверьте Web Service (замените на ваш URL)
curl https://tgbot-web.onrender.com/

# 2. Проверьте API
curl https://tgbot-web.onrender.com/api/get_movie?user_id=123

# 3. Проверьте бота в Telegram
# Отправьте /start и проверьте ответ
```

### 💡 Полезные команды для Render:

- **Автоматический деплой:** Просто делайте `git push` в GitHub
- **Ручной деплой:** Dashboard → ваш сервис → Manual Deploy
- **Просмотр логов:** Dashboard → ваш сервис → Logs
- **Остановка сервиса:** Dashboard → ваш сервис → Settings → Suspend
- **Удаление сервиса:** Dashboard → ваш сервис → Settings → Delete

---

## 🖥️ Деплой на VPS (Ubuntu/Debian)

### Шаг 1: Подготовка сервера

```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите Python и зависимости
sudo apt install python3 python3-pip python3-venv nginx certbot python3-certbot-nginx -y
```

### Шаг 2: Настройка проекта

```bash
# Создайте директорию
mkdir -p /opt/tgbot
cd /opt/tgbot

# Скопируйте файлы проекта
# (используйте git clone или scp)

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### Шаг 3: Настройка systemd для бота

Создайте файл `/etc/systemd/system/tgbot.service`:

```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/opt/tgbot
Environment="PATH=/opt/tgbot/venv/bin"
ExecStart=/opt/tgbot/venv/bin/python /opt/tgbot/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

### Шаг 4: Настройка systemd для веб-сервера

Создайте файл `/etc/systemd/system/tgbot-web.service`:

```ini
[Unit]
Description=Telegram Bot Web Server
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/opt/tgbot
Environment="PATH=/opt/tgbot/venv/bin"
ExecStart=/opt/tgbot/venv/bin/python /opt/tgbot/web_server.py
Restart=always

[Install]
WantedBy=multi-user.target
```

### Шаг 5: Настройка Nginx

Создайте файл `/etc/nginx/sites-available/tgbot`:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Активируйте конфигурацию:

```bash
sudo ln -s /etc/nginx/sites-available/tgbot /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### Шаг 6: Настройка SSL (HTTPS)

```bash
# Получите SSL сертификат
sudo certbot --nginx -d your-domain.com

# Certbot автоматически обновит конфигурацию Nginx
```

### Шаг 7: Запуск сервисов

```bash
# Создайте .env файл
cd /opt/tgbot
nano .env
# Добавьте BOT_TOKEN и KINOPOISK_API_KEY

# Запустите сервисы
sudo systemctl enable tgbot
sudo systemctl enable tgbot-web
sudo systemctl start tgbot
sudo systemctl start tgbot-web

# Проверьте статус
sudo systemctl status tgbot
sudo systemctl status tgbot-web
```

### Шаг 8: Обновление URL

Обновите URL в:
1. `bot.py` - `web_app_url = "https://your-domain.com/"`
2. `webapp/app.js` - `const API_URL = 'https://your-domain.com/api'`

Перезапустите сервисы:
```bash
sudo systemctl restart tgbot
sudo systemctl restart tgbot-web
```

---

## 🔧 Настройка мини-приложения в BotFather

После деплоя нужно настроить Web App в BotFather:

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Menu Button"
5. Выберите "Configure Menu Button"
6. Введите текст кнопки (например, "Открыть приложение")
7. Введите URL вашего веб-приложения (например, `https://your-domain.com/`)

---

## ✅ Проверка работы

1. **Проверьте бота:**
   - Откройте бота в Telegram
   - Отправьте `/start`
   - Проверьте, что меню открывается

2. **Проверьте мини-приложение:**
   - Нажмите кнопку "Открыть мини-приложение" в боте
   - Проверьте, что приложение открывается
   - Попробуйте свайпы (вправо - в избранное, влево - пропустить)

3. **Проверьте избранное:**
   - Добавьте фильм в избранное
   - Откройте раздел "Избранное" в боте
   - Проверьте, что фильм сохранился

---

## 🐛 Решение проблем

### Бот не запускается
- Проверьте, что `.env` файл создан и содержит правильные ключи
- Проверьте логи: `heroku logs --tail` (для Heroku) или `journalctl -u tgbot -f` (для systemd)

### Мини-приложение не открывается
- Убедитесь, что используется HTTPS (не HTTP)
- Проверьте, что URL правильный в BotFather
- Проверьте консоль браузера на ошибки (F12)

### API не работает
- Проверьте, что веб-сервер запущен
- Проверьте логи веб-сервера
- Убедитесь, что API_URL в `app.js` правильный

### База данных не работает
- Проверьте права доступа к файлу `favorites.db`
- Убедитесь, что файл создается в правильной директории

---

## 📝 Структура файлов после деплоя

```
tgbot/
├── bot.py                 # Основной файл бота
├── web_server.py          # Веб-сервер для мини-приложения
├── database.py            # Работа с базой данных
├── kinopoisk_api.py       # API Кинопоиска
├── wink_api.py            # API Wink
├── config.py              # Конфигурация
├── webapp/                # Мини-приложение
│   ├── index.html
│   ├── style.css
│   └── app.js
├── .env                   # Переменные окружения
├── favorites.db           # База данных (создается автоматически)
├── requirements.txt
├── Procfile               # Для Heroku
└── runtime.txt            # Для Heroku
```

---

## 🎉 Готово!

После выполнения всех шагов ваш бот будет работать с:
- ✅ Избранным для каждого пользователя
- ✅ Мини-приложением с свайпами
- ✅ Синхронизацией с Кинопоиском и Wink
- ✅ Красивым интерфейсом

Приятного использования! 🍿

